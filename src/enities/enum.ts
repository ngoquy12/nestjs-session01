export enum HttpMessages {
  SUCCESS_MESSAGE = 'Thành công',
  ERROR_MESSAGE = 'Lỗi hệ thống',
}

export enum HttpStatus {
  SUCCESS = 200,
  NOT_FOUND = 404,
  BAD_REQUEST = 400,
  ERROR_SERVER = 500,
}
